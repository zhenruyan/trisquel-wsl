foreground=0x8AC8FF
background=0xFFFFFF
screen-colour=0x121C37
label normal=Normal
append normal=
label oem=OEM install (for manufacturers)
append oem=oem-config/enable=true
applies oem=live live-install install
label text=Terminal only (no graphics)
append text=systemd.unit=multi-user.target
applies text=live live-install install
